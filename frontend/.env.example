# Copy this file to frontend/.env.local and adjust if needed.

# Where the browser sends API calls. Baked into the bundle at build time, so a
# change here needs a rebuild, not just a restart.
NEXT_PUBLIC_API_URL=http://localhost:4000/api
